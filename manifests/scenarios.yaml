scenarios:
  example-contract-change:
    description: Update a source-of-truth contract and synchronize downstream consumers.
    repos:
      - contract-repo
      - consumer-repo
      - worker-repo
    order:
      - contract-repo
      - consumer-repo
      - worker-repo
    integration_checks:
      - scripts/run-integration-checks example-contract-change
    requires_pr_order: true
